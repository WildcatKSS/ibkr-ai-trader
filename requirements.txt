# =============================================================================
# requirements.txt — IBKR AI Trader
# Python 3.11 · Ubuntu 22.04 LTS
# =============================================================================

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.18       # form/file upload support (>=0.0.18 fixes ReDoS GHSA-2jv5-9r88-3w3p)
itsdangerous>=2.2.0            # session signing

# ── Authentication ────────────────────────────────────────────────────────────
PyJWT[crypto]>=2.8.0               # JWT tokens (replaces python-jose, which has unpatched GHSA-cjwg-qfpm-7377)
passlib[bcrypt]>=1.7.4             # password hashing
pyotp>=2.9.0                       # TOTP for optional 2FA

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.13.0
PyMySQL>=1.1.0                 # MariaDB/MySQL connector (pure Python, no system lib needed)
cryptography>=42.0.0           # required by PyMySQL for SSL connections

# ── Data & machine learning ───────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
lightgbm>=4.3.0
scikit-learn>=1.4.0            # preprocessing and feature utilities used by LightGBM pipeline
ta>=0.11.0                     # technical indicators (bot/signals/indicators.py)

# ── IBKR API ──────────────────────────────────────────────────────────────────
ib_insync>=0.9.86              # async TWS / IB Gateway API wrapper

# ── Claude API ────────────────────────────────────────────────────────────────
anthropic>=0.25.0

# ── Market data & news ────────────────────────────────────────────────────────
# The sentiment module (bot/sentiment/) uses the requests library to call the
# Alpaca News and Finnhub REST APIs directly — no SDK packages needed.
requests>=2.31.0               # HTTP client for Alpaca News + Finnhub sentiment APIs

# ── Trading calendar ──────────────────────────────────────────────────────────
# NYSE trading calendar — imported exclusively via bot/utils/calendar.py
# exchange_calendars is the actively maintained fork of the abandoned
# trading_calendars package; it is API-compatible and supports Python 3.11+.
exchange_calendars>=4.0

# ── HTTP & async I/O ──────────────────────────────────────────────────────────
httpx>=0.27.0                  # async HTTP client (webhook delivery, external API calls)

# ── Environment ───────────────────────────────────────────────────────────────
python-dotenv>=1.0.0           # loads /opt/ibkr-trader/.env at startup

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0,<1.0.0    # 1.x introduced event loop hangs with sync tests
pytest-mock>=3.14.0
